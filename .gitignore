# dependencies
node_modules/

# builds
.next/
out/
dist/
build/

# env — secrets live in .env.local, never committed (env.example is the template)
.env
.env.local
.env.*.local

# spine-proof output (generated client snapshot + minted delivery token)
scripts/out/

# XVibe server-side state: app workspaces, delivery-token custody, deploys
.xvibe/

# misc
.DS_Store
*.log
*.tsbuildinfo
