# Open — discussed, not yet decided or blocked

| # | Question | Unblocked by |
|---|---|---|
| 1 | **"go P0"** — approve Sonnet-5 build tier (~3× build cost)? | operator word |
| 2 | Task-management product: app built ON XVibe, or studio-internal tool first? (Replit-style task-on-a-copy + PRD builder is planned either way — #16) | operator pick |
| 3 | Pricing/tiers: free = preview-only vs published-with-badge; per-app price point (mirror-Pluggie model sketched 07-30) | operator; needs Pluggie per-project billing shape |
| 4 | How loudly does XVibe say "runs on Pluggie"? (brief-era open question, still open) | operator |
| 5 | Does XVibe cannibalize the agency wedge? (brief-era, still open) | operator |
| 6 | `*.xvibe.app` wildcard records + env flip (apps currently at `*.apps.xvibe.app`) | operator: 3 CNAMEs + Render domain + env change |
| 7 | Clerk staff-role claims (token template + publicMetadata) — real role gating vs "any signed-in user" demos | operator: Clerk dashboard setup |
| 8 | Pluggie repo path on this machine (for the Build & deploy button, #8) | operator |
| 9 | Support-inbox test: pipeline move + reload persistence + resolved-email in Logs — final unchecked boxes | operator: 2 minutes in the app |
| 10 | Token rotation (mcp + Anthropic key traveled through several dashboards) | operator |
| 11 | Sonnet vs Haiku for the eval sweeps (cost/fidelity tradeoff) | decided with #1 |
